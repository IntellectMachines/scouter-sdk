# Optional: override the default Scouter cloud endpoint (e.g. for self-hosted)
# SCOUTER_BACKEND_URL=http://localhost:8000

# Your Scouter API key (from the dashboard)
SCOUTER_API_KEY=your-api-key-here
